# Environment example for local development
## Copy this file to `.env.local` and fill the values

# Supabase (public, safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase (server-only, DO NOT prefix with NEXT_PUBLIC)
# Optional: needed for admin tasks (invoking Edge Functions with elevated perms, service operations)
# Keep this only in server environments and local dev; never expose to the browser.
SUPABASE_SERVICE_ROLE=your_service_role_key
SUPABASE_DB_PASSWORD=your_supabase_db_password